<{{h}}{{#with hc}} class="{{this}}"{{/with}}>
  {{ this.frontMatter.title }}
</{{h}}>

<{{l}}{{#with lc}} class="{{this}}"{{/with}}>
{{ this.frontMatter.description }}&nbsp;
</{{l}}>

<p>
  <span class="badge badge-primary">{{this.frontMatter.type}}</span>
  <span class="badge badge-secondary">{{this.frontMatter.category}}</span>
{{~#each this.frontMatter.tags as |t| }}
  <span class="badge badge-info">{{t}}</span>
{{/each~}}
</p>
