<h1>
    {{#with context.frontMatter.title }}
    {{ this }}
    {{else}}
    {{ compact.site.title }}
    {{/with}}
</h1>

{{#with context.frontMatter.lead}}<p class="lead">{{ this }}</p>{{/with}}